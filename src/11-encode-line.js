/**
 * Given a string, return its encoding version.
 *
 * @param {String} str
 * @return {String}
 *
 * @example
 * For aabbbc should return 2a3bc
 *
 */
function encodeLine(/* str */) {
  /* const arr = str.split('');
  const result =[] ;

  for (let i = 0; i < arr.length; i++) {
    if ([arr[i]] === undefined) { obj[arr[i]] = 1; } else { obj[arr[i]]++; }
  }

  if (obj[arr[i]] === 1) { result += [arr[i]]; } else { result += (obj[arr[i]] + [arr[i]]); }
  return result; */
}


module.exports = encodeLine;
